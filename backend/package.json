{
  "name": "backend",
  "version": "1.0.0",
  "description": "backend API for esp32 controls",
  "main": "dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "compile": "tsc -w",
    "dev": "npx tsc && node dist/index.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^17.2.1",
    "express": "^5.1.0",
    "express-ws": "^5.0.2"
  },
  "devDependencies": {
    "@types/express": "^5.0.3",
    "@types/express-ws": "^3.0.5",
    "@types/node": "^24.3.0"
  }
}
